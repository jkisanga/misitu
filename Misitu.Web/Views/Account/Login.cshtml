@using Abp.Web.Mvc.Extensions
@using Microsoft.Owin.Security
@model Misitu.Web.Models.Account.LoginFormViewModel
@{
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
    
}


@section Scripts
{
    @Html.IncludeScript("~/Views/Account/Login.js")
}
              
                        <!-- Login form -->
                        <form id="LoginForm" class="form-horizontal" action="@(Url.Action("Login", "Account"))" method="POST">
                          
                            @*@if (Model.IsMultiTenancyEnabled)
                            {
                                <div class="form-group">
                                    <div class="col-lg-12">
                                        <input type="text" class="form-control" id="TenancyName" placeholder="@L("TenancyName")" required maxlength="@Misitu.MultiTenancy.Tenant.MaxTenancyNameLength">
                                        <span class="text-muted">@L("CanBeEmptyToLoginAsHost")</span>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <input type="hidden" id="TenancyName" value="">
                            }*@
                            <!-- Email -->
                            <div class="form-group">
                                <label class="control-label col-lg-3" for="inputEmail">Username</label>
                                <div class="col-lg-9">
                                    <input type="text" class="form-control" id="EmailAddressInput" placeholder="@L("UserNameOrEmail")" required maxlength="@Misitu.Users.User.MaxEmailAddressLength"/>
                                </div>
                            </div>
                            <!-- Password -->
                            <div class="form-group">
                                <label class="control-label col-lg-3" for="inputPassword">Password</label>
                                <div class="col-lg-9">
                                    <input type="password" class="form-control" id="PasswordInput" placeholder="@L("Password")" required maxlength="@Misitu.Users.User.MaxPlainPasswordLength">
                                </div>
                            </div>
                            <!-- Remember me checkbox and sign in button -->
                            <div class="form-group">
                                <div class="col-lg-9 col-lg-offset-2">
                                    <div class="checkbox">
                                        <label>
                                            <input type="checkbox"> Remember me
                                        </label>
                                    </div>
                                </div>
                            </div>
                            <input id="ReturnUrl" type="hidden" name="ReturnUrl" value="@Model.ReturnUrl" />
                            <input id="ReturnUrlHash" type="hidden" name="returnUrlHash" />
                            <div class="col-lg-9 col-lg-offset-2">
                                <button type="submit" id="LoginButton" class="btn btn-danger"><i class="fa fa-sign-in"></i> @L("LogIn")</button>

                                @Html.ActionLink("New Account", "Account", "Applicant", new { area = "Client" },null)
                             
                            </div>
                            <br />
                        </form>
                      
                        